# Update these with your Supabase details from your project settings > API
# https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key

# Required for seed/migration scripts that write to the DB under RLS.
# Keep this key server-side only (never expose to the browser, never commit real values).
# https://app.supabase.com/project/_/settings/api
SUPABASE_SERVICE_ROLE_KEY=your-service-role-or-secret-key

# Optional: enables traffic congestion snapshots in LiveConditions.
# Create a key in TomTom Developer Portal.
TOMTOM_API_KEY=your-tomtom-api-key

# Optional: used to build absolute metadata URLs in Next.js.
# Vercel provides this automatically in deployed environments.
VERCEL_URL=
